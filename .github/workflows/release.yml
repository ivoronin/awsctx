name: Release
on:
  push:
    tags: ['v*']
jobs:
  release:
    uses: ivoronin/github-workflows/.github/workflows/release.yml@main
    with:
      language: go
      docker: false
      brew: true
      krew: false
    secrets: inherit

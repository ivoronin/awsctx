version: "2"

linters:
  default: all
  disable:
    - exhaustruct
    - depguard
    - wrapcheck
    - tagalign
    - forbidigo
    - perfsprint
    - gochecknoglobals
    - wsl  # deprecated
    - revive  # too noisy
